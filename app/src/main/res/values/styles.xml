<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- Line separators -->

    <style name="dividerHorizontal">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>

    </style>

    <style name="dividerVertical">
        <item name="android:layout_width">1dp</item>
        <item name="android:layout_height">match_parent</item>
    </style>

    <!-- Text styles -->
    <style name="profileCollapsableContentText">
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="profileScrollableContentText">
        <item name="android:padding">8dp</item>
    </style>

    <style name="profileScrollableContentTitleText" parent="profileScrollableContentText">
        <item name="android:textSize">18sp</item>
    </style>

    <style name="profileScrollableContentDescriptionText" parent="profileScrollableContentText">
        <item name="android:textSize">15sp</item>
    </style>

</resources>
